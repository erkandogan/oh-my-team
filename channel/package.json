{
  "name": "omt-channel",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "bridge": "bun run bridge.ts",
    "router": "bun run router.ts",
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "node-pty": "^1.0.0",
    "@xterm/xterm": "^5.5.0",
    "@xterm/addon-fit": "^0.10.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10"
  }
}
